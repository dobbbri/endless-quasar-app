const config = {
  dark: 'false',
  notify: {
    position: 'top',
    color: 'dark',
    iconSize: 'md',
    textColor: 'white',
    progress: true,
    actions: [{ color: 'white', rounded: true, dense: true }]
  }
}

export default config
