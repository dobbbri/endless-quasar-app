const config = {
  dark: 'false',
  notify: {
    position: 'top',
    color: 'dark',
    iconSize: 'md',
    textColor: 'white',
    progress: true,
    actions: [{ icon: 'close', iconSize: 'xs', color: 'grey-7', dense: true }]
  }
}

export default config
